<%= form_for(@start_article) do |f| %>
  <% if @start_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@start_article.errors.count, "error") %> prohibited this start_article from being saved:</h2>

      <ul>
      <% @start_article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
